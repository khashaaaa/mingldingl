/** Mirrors `DTOs/FieldLimits.cs` on the engine — keep the two in step. */
export const FIELD_LIMITS = {
  displayName: 64,
  bio: 200,
  message: 2000,
  minAge: 18,
  maxAge: 99,
  maxPhotos: 6,
} as const;
